# MVP Build — Completed Scaffold

The MVP Flutter codebase lives in `lib/`. Run `flutter create .` then `flutter pub get` if platform folders are missing.

See `README.md` for Firebase and run instructions.
